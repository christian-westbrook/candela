# Candela

An advent calendar for Christmas movies!

## Usage

Create a file called `movies.txt` in the same directory as `candela.rb`. Each line of `movies.txt` should contain the title of a film that you would like to be a part of your advent calendar.
